seata:
  config:
    type: nacos
    nacos:
      server-addr: host.docker.internal:8848
      group: SEATA_GROUP
      data-id: seataServer.properties
  registry:
    type: nacos
    nacos:
      server-addr: host.docker.internal:8848
      group: SEATA_GROUP
      application: seata-server
      cluster: default
